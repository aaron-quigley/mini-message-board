<!DOCTYPE html>
<html>
	<head>
		<title><%= title %></title>
		<link rel="stylesheet" href="/stylesheets/style.css" />
	</head>
	<body>
		<main>
			<div class="messageBoard">
				<h1><%= title %></h1>
				<div class="link"><a href="./new">Add a message</a></div>
				<% locals.messages.forEach(message => { %>
				<div class="message">
					<h4><%= message.user %></h4>
					<p><%= message.text %></p>
					<p><%= message.added %></p>
				</div>
				<% }) %>
			</div>
		</main>
	</body>
</html>
